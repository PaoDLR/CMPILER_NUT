expose sleep testCode{

    expose shiv kassadin main(){
    digimon  x = 0;
    scan("Input a number: ", x);

    iv(digimon i; i< 10; i+=1){
        printing("Value: " +x+);
        x = 5x;
    }

    can(x = 4){
        printing(x is a very huge number);
    }

    digimon i = 0;
    glaz{
        i++;
    }during(i == x);

    digimon sum = x + y / z;

    printing("This is not allowed")
    printing("Is this allowed")

    string_string = "This is a string"
    drown value = (5 * 3) + (5 * 5 / 1 + (4 + (5 % 3)))/2.0 * 8.0;
    drown nextValue = 5 -- '[5 + 5 ++ 5 ** 5;
    }
}